[project]
name = "mediaferry-protocol"
version = "0.0.0"
description = "Wire protocol shared by mountd and the mediaferry app"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mediaferry_protocol"]
