# 開発の記録

**ここにあるのは「どう作ったか」であって、「いまどうなっているか」ではありません。**

現在の仕様は [`../design.md`](../design.md)、そう決めた理由は
[`../decisions.md`](../decisions.md) にあります。**このディレクトリを読まなくても、
現在の姿は分かります。**

| ファイル | 中身 |
| --- | --- |
| `phase0-findings.md` | 実装の前に測った結果。**理屈で決めずに測った値そのもの**（fd の受け渡し、Immich API、28 GiB のアップロード） |
| `phase1-plan.md` | 基盤と取り込み。公開プロトコル、DB スキーマ、crash consistency |
| `phase2-plan.md` | 分割動画の結合。検出・ffmpeg・検証・選択肢 |
| `phase3-plan.md` | Immich 同期。状態機械、転送先、`origin` 判別、タグ、日時 |
| `phase4-plan.md` | Web UI。認証・CSRF・SSE・8 画面・E2E |
| `phase5-plan.md` | 汎用化。Canon、プロファイル編集、複数デバイス |
| `phase6-plan.md` | RAW/JPEG のスタッキング、`UPLOAD_CONCURRENCY` の撤去 |
| `phase7-plan.md` | 画面の作り直し。3 つのタブと「やること」、送る動線、旧 8 画面の行き先 |
| `phase7-record.md` | **Phase 7 をどう回したか**。13 タスクの巡数と見つかったもの、**裁定 1〜58 の全文**、変異試験の記録（レビュアーが自分で当てて生き残った数、機械的な数え上げへの切り替え、検出できないものとその理由）、「テストが緑なのに壊れていた」不具合、**実機で触ってからの一区切り**（自動テストが全部緑のまま利用者が見つけた表示の破綻 6 件と、それぞれに足した網。受け入れ 8 本 → 16 本）、**PR 全体のレビュー**（`/code-review max` が全部緑の状態から見つけた 15 件と、そのうち 3 件が「テストが思い違いを仕様として固定していた」ものだったこと） |
| `phase7-rulings.md` | **Phase 7 の裁定の番号の索引**。コードのコメントが引いている「裁定 N」が何を決めたのか。**全文は `phase7-record.md`** |
| `phase8-design.md` | **ホームが「いま何が起きているか」を言えるようにする設計**（2026-08-24）。実機で利用者が見つけた 3 件を 1 つの動線として閉じる。モックで A-2 を選んだ理由、`/devices` に足す 3 欄、送信の進捗を心拍に相乗りさせる形、watcher が `scan` を積まない件 |
| `phase8-plan.md` | Phase 8 の実装計画。10 タスクのブリーフ、`homeSections` の導出規則、`0020_auto_scan.sql` |
| `phase8-record.md` | **Phase 8 をどう回したか**。10 タスクの巡数と見つかったもの、**変異試験の記録**（当てた 50 件・生き残った 15 件・足したテスト 7 本・**等価変異と「死んだ定義」が変異試験に嘘をつくこと**）、**申し送られた偽テスト 2 件**（`also=` を消しても通るテストと、テストが 1 本も当たっていなかった `ownership_errors`）、**書き換えたテストが守るものを緩めていた 2 件**、**E2E が受け入れコマンドに入っていなかったので 8 タスクぶん赤だったこと**、計画の誤り 3 件、モックで A-2 を選んだ理由 |
| `phase9-design.md` | **写真タブで 1 件を知って消せるようにする設計**（2026-08-25）。実機で見つかった 4 つの症状を 1 つの動線として閉じる。モックで選んだ 3 つ（押す＝開く・専用の「くわしく」画面・記録は写真タブへ寄せる）とその理由、**消せる／消せないの規則**（Immich に生きていないものだけ・「無い」には観測を要求する）、消したときにグループを `skipped` にする理由 |
| `phase9-plan.md` | Phase 9 の実装計画。10 タスクのブリーフ、`LIVING_REMOTE_CLAUSE` と `deletion_blocker`、`discard()` を「開く版／開いている前提の版」に割る理由（`immediate()` は入れ子にできない）、タイルの四隅の割り当て |
| `phase9-record.md` | **Phase 9 をどう回したか**。10 タスクの巡数と見つかったもの、**索引の顛末**（実測 57.7 ms → 全体索引で他経路が 74 ms へ退行 → 部分索引で解消。「測る対象が足りていなかった」教訓）、**変異試験の記録**（56 件当てて構造的に生き残ったのは 2 件）、**実装者の「すべて検出」の報告が実態と食い違った 2 件**（レビュアーが独立に変異を当て直して発覚）、**統括の裁定そのものが間違っていて実装者が指摘して直った件**、計画の誤り、**作業ディレクトリの事故**（`git checkout`／`git add -A` を全面禁止した理由） |
| `phase7-prototype.html` | **Phase 7 の見た目の参照物**（自己完結。ブラウザで開いて押せる）。色・間隔・文言・アイコンはここから取る |
| `phase10-design.md` | **送る前に「どれとどれが 1 枚か」を見せる設計**（2026-08-25）。実機で 70D の RAW+JPEG を取り込んだ利用者が見つけた 3 症状。スタックの規則を**身元と資格に割る**理由、**身元から時刻を外した**理由（偽陰性しか生まない／`UNIQUE (volume_instance_id, rel_path)` が偽陽性を構造的に防ぐ）、それでも足りない**片方だけ撮り直したときの誤組**と、それを塞ぐ**同席の印**。一覧を「畳む」のではなく「従を隠す」形にした理由（ページの境目を組がまたぐ） |
| `phase10-plan.md` | Phase 10 の実装計画。8 タスクのブリーフ、**同席の印を `<job_id>:<stem prefix>` に直した理由**（スキャンの id だけだと別々の組が同じ印になる）、`tolerance_seconds` を**許して読まない**理由（既存リビジョンの JSON に入っているので弾くと DB が開けない）、従外しの SQL と拡張子の順位表 |
| `hardware-verification.md` | **実機検証の記録**（2026-08-20〜）。実測した値、実機でしか出なかった不具合 10 件、ffmpeg の実挙動、**Canon EOS 70D の実カード**（13・14 番、`hints` が当たらなかったこと） |

## なぜ捨てないのか

**この 3 つは、ここにしか無いからです。**

1. **変異試験の記録。** どの変異が検出できて、どれが構造的に検出できないかを
   1 件ずつ書いてあります。**「このテストは冗長だから削ってよい」と判断する前に
   読むもの**です（対で壊せば検出できる冗長は、意図であって無駄ではありません）。
2. **レビューの記録。** 30 巡ぶんの指摘と、それを妥当と判断した根拠、退けた根拠が
   残っています。同じ層をもう一度触るときに、何が見落とされやすいかが分かります。
3. **試したが採らなかった案とその理由。** 「なぜ別のやり方にしなかったか」は
   仕様書には書かれません。

## 読むときの注意

- **コードの引用は書かれた時点のものです。** 現在の実装とは違うことがあります
- **「次にやること」「未着手」といった記述は当時のものです。** 現在地は
  [`../development.md`](../development.md) を見てください
- **`HANDOFF.md` や `docs/phaseN-plan.md` への参照が出てきます。** 前者は
  [`../development.md`](../development.md) と [`../decisions.md`](../decisions.md) に
  分かれ、後者はこのディレクトリに移りました
- ここに書かれた判断が現在も有効かどうかは、[`../decisions.md`](../decisions.md)
  が正です。**食い違ったら `decisions.md` を信じてください**
