-- リモートの撮影日時の観測（§9.10 / §13）。
--
-- 承認の画面は「現在値と変更案を並べて表示」する。現在値はどこかに保存されて
-- いなければ出せない —— 画面を開くたびに N 件ぶんの HTTP を出すわけにはいかない。
--
-- **いつ時点の観測かは `remote_checked_at` が持つ**（列を増やさない）。日時と
-- 観測時刻を別々に書くと、「日時は新しいが観測時刻は古い」行ができる。

ALTER TABLE upload_record ADD COLUMN remote_datetime_original TEXT;
