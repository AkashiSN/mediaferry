# CLAUDE.md

カメラの SD カードを NAS へ取り込み、分割動画を結合して Immich へ同期する
TrueNAS カスタムアプリ。

**まず [`docs/development.md`](docs/development.md) を読む。** 環境の癖、開発コマンド、
テストとレビューの作法がそこにある。設計の正本は [`docs/design.md`](docs/design.md)、
**そう決めた理由**は [`docs/decisions.md`](docs/decisions.md)、**どう作ったか**の記録は
[`docs/history/`](docs/history/README.md)。

## コメントとドキュメントの棲み分け（重要）

コードのコメントと `docs/` のドキュメントは役割を分ける。

- **コード内コメント**は、**いま書かれているコードを現在形で説明する**。その処理が
  「何をするか / なぜそうしているか（現在の理由）」に限る。**過去の経緯は書かない**：
  「以前は〜だった」「〜へ移行した」「かつて〜していた」「過去に〜で失敗した」「廃止した
  旧方式」などは、コメントから除く。将来のための注意（「〜が再発したら〜する」等）は
  現在の判断として簡潔に残してよい。
- **過去の経緯・変更履歴・移行の記録**は、**`docs/` に残す**（失わない）。設計変更の背景を
  コードから消すときは、対応する docs に経緯が書かれているか確認し、無ければ docs 側へ移す。

判定の目安: 「〜だった窓」のように*現在の対象*を指す表現や、「〜へ倒す」のような*現在の
動作説明*は経緯ではない（そのまま残す）。

**コメントと docstring は日本語で書く。**

## コーディング規約

- Python は `>=3.14`。実際に使う版は `.python-version` で 3.14 に固定してある
  （配るイメージと同じ版で試すため）。ruff の設定は `pyproject.toml` にある
  （`line-length = 100`、`select = ["E", "F", "I", "UP", "B", "SIM", "ANN", "S"]`）
- **`docs/` は ruff の対象外**（`extend-exclude`）。ruff は Markdown 内のコードブロックも
  整形するので、外すと仕様書と実装計画そのものが書き換わる
- すべてのモジュールは `from __future__ import annotations` で始める
- **環境固有の値をリポジトリに含めない。** IP アドレス、ホスト名、データセットのパス、
  API キー、タイムゾーンの実値をコードにもテストにも書かない
- **DB に絶対パスを保存しない。** `DATA_ROOT` からの相対パスのみが正規形
- **秘密をログ・`job.params_json`・`job_event`・API 応答・例外メッセージに出さない**
- 外部コマンドは必ず引数配列で起動する。シェル文字列を組み立てない
- ソース側のパス解決は dirfd 起点の**単一構成要素のみ**。`..`・絶対パス・シンボリック
  リンクを辿らない（`O_NOFOLLOW`）
- システム時刻は **UTC の ISO-8601 文字列**で DB に入れ、生成は `mediaferry.clock` の
  関数だけを使う。**例外は `media_file.captured_at`**（解決したオフセット付きで保存する。
  UTC へ正規化すると `force_offset` で復元した現地の壁時計が読めなくなる）
- **DB 接続はスコープごとに 1 本。** トランザクションは接続に属していてスレッドには
  属さない。詳細は `docs/decisions.md`

## 開発コマンド

```bash
uv sync --all-packages   # --all-packages が必須。素の sync ではメンバーが入らない
uv run pytest
uv run pytest -m needs_root   # ユーザ名前空間が使える環境でのみ通る
uv run ruff check .
uv run ruff format --check .
```

`--all-packages` が要るのは、根の `mediaferry-workspace` がメンバーに依存していないため。

## テストの作法

- 実装より先に**失敗するテストを書き、失敗を確認してから**最小実装する
- **変異試験を省かない。** 実装の判断を 1 つずつ壊し、対応するテストが落ちることを
  確認してから戻す。Phase 1 では「通ってはいるが実装の判断を検証していない」テストが
  30 件以上見つかった。よくある 3 パターンは `docs/development.md` にある
- **変異試験は `PYTHONDONTWRITEBYTECODE=1` を付ける。** 変異の前後でバイト数が変わらない
  書き換え（`>` を `<`、`a or b` を `b or a` 等）では `.pyc` の無効化条件をすり抜けて
  古いバイトコードが使われ、変異が効いているかを読み違える
- **検出できない変異は、検出できないことを記録に残す。** 構造的にテスト不能な保険が
  実在する（例: `claim_next` の CAS 条件は `BEGIN IMMEDIATE` が claimer を直列化するので
  到達しない）
- マーカー: root を要するものは `needs_root`、実 Immich を要するものは `needs_immich`。
  既定の `pytest` では実行されない

## コミット

Conventional Commits + 日本語の本文。**なぜそうしたか**を本文に残す。この
リポジトリでは、判断の理由がコミット本文と `docs/` にしか無い。
